# OpenAI API Key for LLM interactions
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Ollama API endpoint (if using Ollama backend)
# OLLAMA_API_ENDPOINT=http://localhost:11434/api

# Optional: Path to dataset for evaluation
# DATASET_PATH=data/locomo10.json

# Optional: Model to use for evaluation
# MODEL_NAME=gpt-4o-mini

# Optional: Backend to use (openai or ollama)
# LLM_BACKEND=openai

# Optional: Ratio of dataset to evaluate (0.0 to 1.0)
# EVAL_RATIO=0.1
